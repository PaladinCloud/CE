import { PropValueLinkDirective } from './prop-value-link.directive';

describe('PropValueLinkDirective', () => {
    it('should create an instance', () => {
        // const directive = new PropValueLinkDirective();
        // expect(directive).toBeTruthy();
    });
});
