import { NotificationDetailsPipe } from './notification-details.pipe';

describe('NotificationDetailsPipe', () => {
    it('create an instance', () => {
        const pipe = new NotificationDetailsPipe();
        expect(pipe).toBeTruthy();
    });
});
