import { ResultCountPipe } from './result-count.pipe';

describe('ResultCountPipe', () => {
  it('create an instance', () => {
    const pipe = new ResultCountPipe();
    expect(pipe).toBeTruthy();
  });
});
