import { TableColumnWidthPipe } from './table-column-width.pipe';

describe('TableColumnWidthPipe', () => {
    it('create an instance', () => {
        const pipe = new TableColumnWidthPipe();
        expect(pipe).toBeTruthy();
    });
});
