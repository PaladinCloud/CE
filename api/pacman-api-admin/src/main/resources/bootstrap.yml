security: 
  oauth2:
    resource:
      jwk:
        key-set-uri: https://cognito-idp.us-east-1.amazonaws.com/us-east-1_fzF7DLUkO/.well-known/jwks.json
      client:
        clientId: 1tt4q1vkgf2buhq1gv614nosqs
spring:
  application:
    name: admin-service
    title: Pacman Service
    description: Pacman Admin API provides admin capabilities
  cloud:
    config:
      uri: https://dev.paladincloud.io/api/config
      enabled: true
      fail-fast: true
      name: api,admin-service,job-scheduler,batch
      password: pacman
      username: user
      label: latest
  jpa: 
    hibernate:
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
  profiles:
    active: ${ENVIRONMENT:prd}
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB
tenable:
  user-agent: Integration/1.0 (Paladin Cloud; Paladin Cloud; Build/1.0)
